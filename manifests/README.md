# Manifests

This folder is reserved for public-safe example manifests only.

Current status:

```text
manifest_import_allowed=false
real_service_records_allowed=false
real_mcp_records_allowed=false
real_drive_pointers_allowed=false
real_proof_records_allowed=false
```

Future manifests must be empty, synthetic, prepared-only, or public-safe derivatives with private lineage and approval.
